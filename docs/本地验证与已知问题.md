# 本次部署验证与现有缺陷

基线：`132a8281`，测试针对本地部署与原仓库业务代码。新增部署脚本没有修改业务实现。

## 已通过的部署与回归检查

| 检查 | 本次结果 | 证据 |
| --- | --- | --- |
| Maven package | BUILD SUCCESS；仓库没有 src/test 用例 | ../deploy/logs/backend-build.log |
| Vite production build | 通过，VITE_USE_MOCK=false | ../deploy/logs/frontend-build.log |
| 本地回归执行器 | 5 组通过（类型、经费、选择器、看板、备案、表单维护等） | ../deploy/logs/platform-checks.log |
| 本地真实接口巡检 | 96 PASS，0 WARN，0 FAIL | ../deploy/logs/task-flow-live.log |
| 无项目范围账号写入阻断 | 12 PASS，0 FAIL；只验证既有脚本所列接口和不存在 ID | ../deploy/logs/write-permission.log |
| MinIO 实际上传/下载/删除 | 中文文件名与字节一致，临时文件已清理 | ../deploy/logs/file-roundtrip.log |
| 我的已办 | 7 PASS，0 FAIL；当前样本已办为空，内容归属断言覆盖有限 | ../deploy/logs/my-done.log |
| 经费核销巡检 | 无失败，1 WARN：样本无待核销待办 | ../deploy/logs/fund-writeoff.log |

## 重启与数据持久化检查

浏览器实际登录和 11 个关键业务路由渲染通过，无页面 JavaScript 异常、HTTP API 失败或业务错误码。范围包括一本账、看板、申报、备案、里程碑、经费、验收、转化、后评价、用户与权限矩阵；这是页面冒烟检查，没有逐个提交业务表单。证据：[浏览器结果](../deploy/logs/browser-smoke.json)、[工作台截图](../deploy/logs/browser-dashboard.png)、[台账截图](../deploy/logs/browser-ledger.png)。

完整 stop/start 后，16 个账号登录、资料读取、后端登出及旧 token 401 拒绝均通过。两个项目和八个样本附件保留，附件经后端下载后逐字节比较通过。创建唯一临时项目，经 API 删除后直接查库确认 `deleted=1`，列表查询不可见；测试完成后只清理了该临时行。手工基线备份生成、下载及 SHA-256 校验通过，备份只包含数据库和附件 key 清单，没有执行恢复演练。

证据：[持久化检查](../deploy/logs/persistence-check.log)、[检查结果和备份编号](../deploy/logs/persistence-check.json)、[启停日志](../deploy/logs/restart.log)。

## V19 深层业务测试

以下均为本次重新执行的结果；不是读取仓库历史结果冒充本次验证。功能测试使用真实 Vue 代码与 API 替身；流程图测试执行真实工具函数；Java 测试执行真实控制器与内存 Mapper，不包含真实 SQL/事务/鉴权过滤器。失败表示对应断言未满足，不能把全部失败等同于已验证的生产漏洞。

| 套件 | PASS | FAIL | BLOCKED |
| --- | ---: | ---: | ---: |
| v19-function-results.json | 13 | 13 | 0 |
| v19-flow-view-results.json | 4 | 9 | 0 |
| v19-workflow-results.json | 22 | 13 | 2 |

## v19-function-results.json 未通过项

### FN-AC04 · 空校验结果不得提示校验通过 · FAIL

源码：`frontend/src/views/acceptance/Acceptance.vue`

空数组校验返回了成功提示，和不可提交状态矛盾

true !== false


### FN-AC05 · 项目切换加载期间旧校验立即失效 · FAIL

源码：`frontend/src/views/acceptance/Acceptance.vue`

新项目使用旧项目的前置校验结果发出了提交请求

1 !== 0


### FN-AC06 · 快速切换项目晚返回结果不得覆盖新项目 · FAIL

源码：`frontend/src/views/acceptance/Acceptance.vue`

旧项目异步响应覆盖了当前验收信息

101 !== 202


### FN-AC07 · 连续点击验收提交防止重复请求 · FAIL

源码：`frontend/src/views/acceptance/Acceptance.vue`

验收提交缺少处理中保护，连续点击发送两次请求

2 !== 1


### FN-AC08 · 未选择真实文件不能标记材料已上传 · FAIL

源码：`frontend/src/views/acceptance/Acceptance.vue`

只携带 fieldCode 就登记材料上传，未选择和传输文件

1 !== 0


### FN-AC09 · 无填报权限人员不可执行上传动作 · FAIL

源码：`frontend/src/views/acceptance/Acceptance.vue`

上传处理函数未应用页面岗位授权；后端是否拒绝需集成验证

1 !== 0


### FN-TR02 · 成果名称与所属项目缺失阻止保存 · FAIL

源码：`frontend/src/views/transform/Transform.vue`

必填名称和项目未填写仍发送创建请求

1 !== 0


### FN-TR03 · 未选择项目不能借用首个项目计算权限 · FAIL

源码：`frontend/src/views/transform/Transform.vue`

未选择项目时默认套用了列表第一个项目的权限上下文
+ actual - expected

+ {
+   id: 101,
+   name: '隔离项目A'
+ }
- null


### FN-TR05 · 已绑定但未交付成果不能自动进入已选清单 · FAIL

源码：`frontend/src/views/transform/Transform.vue`

仅禁止点击未交付行，但把既有未交付关联预选进提交清单

'1,2' !== '2'


### FN-TR06 · 无写权限人员不能确认成果关联 · FAIL

源码：`frontend/src/views/transform/Transform.vue`

确认绑定处理函数没有岗位权限拦截；不等同于证明后端可越权

1 !== 0


### FN-TR09 · 已有大于200条成果时不丢失后续页 · FAIL

源码：`frontend/src/views/transform/Transform.vue`

页面强制只取前200条后本地分页，漏掉第201条

false !== true


### FN-IM03 · 评估材料保存不能只登记名称就声称已上传 · FAIL

源码：`frontend/src/views/implement/Evaluation.vue`

仅提交名称、类型、日期和结果，未携带文件却提示材料已上传

true !== false


### FN-IM04 · 未授权人员不能删除评估记录 · FAIL

源码：`frontend/src/views/implement/Evaluation.vue`

评估删除处理函数没有权限拦截；后端数据授权另验

1 !== 0



## v19-flow-view-results.json 未通过项

### FV-02 · 同名但工号不同的人不能代替指定办理人 · FAIL

源码：`frontend/src/utils/flowActor.ts`

工号不匹配后退回姓名匹配，放行同名不同人

true !== false


### FV-03 · 姓名前缀不能冒充完整指定姓名 · FAIL

源码：`frontend/src/utils/flowActor.ts`

startsWith 姓名前缀判定导致错误匹配

true !== false


### FV-04 · 岗位缺失时不能只按角色开放审核 · FAIL

源码：`frontend/src/utils/workDuty.ts`

未指定项目审核人时仍按任职身份开放审核

true !== false


### FV-06 · 绿色展示不能覆盖待审核业务状态 · FAIL

源码：`frontend/src/utils/acceptFlow.ts`

颜色标签被当作审核通过的替代证据

true !== false


### FV-07 · 验收流转图保留全部六类前置检查 · FAIL

源码：`frontend/src/utils/acceptFlow.ts`

当前仅返回4类校验，遗漏外协事项与验收材料齐套

false !== true


### FV-08 · 验收完成状态不能凭空生成已上传附件 · FAIL

源码：`frontend/src/utils/acceptFlow.ts`

仅因验收DONE生成了已上传PDF及默认128KB信息

true !== false


### FV-09 · 缺失验收办理人不得回填固定演示工号 · FAIL

源码：`frontend/src/utils/acceptFlow.ts`

项目团队为空仍显示固定花名册中的办理人

true !== false


### FV-11 · 流程摘要处理人须与当前节点一致 · FAIL

源码：`frontend/src/utils/transformFlow.ts`

当前节点审核人与顶部当前流程摘要使用了不同人员

false !== true


### FV-12 · 缺失成果办理人不得回填固定演示工号 · FAIL

源码：`frontend/src/utils/transformFlow.ts`

无岗位仍按固定演示花名册显示负责人和审核人

true !== false



## v19-workflow-results.json 未通过项

### WF-008 · 已完成里程碑禁止重开提交 · FAIL

源码：`backend/src/main/java/com/comac/rpm/modules/milestone/controller/MilestoneController.java`

Unmodified Java controllers + real FlowAuditGuard, mocked mapper persistence. Expected business rejection but call succeeded; writes=1; audit=SUBMIT:项目负责人提交里程碑销项审核：QA milestone

### WF-016 · 总部总核不得跳过二级财务总核 · FAIL

源码：`backend/src/main/java/com/comac/rpm/modules/fund/controller/FundController.java`

Unmodified Java controllers + real FlowAuditGuard, mocked mapper persistence. Expected business rejection but call succeeded; writes=1;

### WF-021 · 验收缺必填材料禁止提交 · FAIL

源码：`backend/src/main/java/com/comac/rpm/modules/acceptance/controller/AcceptanceController.java`

Unmodified Java controllers + real FlowAuditGuard, mocked mapper persistence. Expected business rejection but call succeeded; writes=1;

### WF-023 · 未申请验收不得直接审核办结 · FAIL

源码：`backend/src/main/java/com/comac/rpm/modules/acceptance/controller/AcceptanceController.java`

Unmodified Java controllers + real FlowAuditGuard, mocked mapper persistence. Expected business rejection but call succeeded; writes=1;

### WF-024 · 验收初审后应进入总部终审而非直接DONE · FAIL

源码：`backend/src/main/java/com/comac/rpm/modules/acceptance/controller/AcceptanceController.java`

Unmodified Java controllers + real FlowAuditGuard, mocked mapper persistence. unit approval must await HQ; actual=DONE;

### WF-025 · 已完成验收禁止重复申请重开 · FAIL

源码：`backend/src/main/java/com/comac/rpm/modules/acceptance/controller/AcceptanceController.java`

Unmodified Java controllers + real FlowAuditGuard, mocked mapper persistence. Expected business rejection but call succeeded; writes=1;

### WF-026 · 验收退回补正应区别于审核在途状态 · FAIL

源码：`backend/src/main/java/com/comac/rpm/modules/acceptance/controller/AcceptanceController.java`

Unmodified Java controllers + real FlowAuditGuard, mocked mapper persistence. return must identify editable correction node; actual=APPLYING;

### WF-027 · 无已交付成果不得新建转化流程 · FAIL

源码：`backend/src/main/java/com/comac/rpm/modules/transform/controller/TransformController.java`

Unmodified Java controllers + real FlowAuditGuard, mocked mapper persistence. Expected business rejection but call succeeded; writes=1;

### WF-028 · 成果未交付禁止绑定 · FAIL

源码：`backend/src/main/java/com/comac/rpm/modules/transform/controller/TransformController.java`

Unmodified Java controllers + real FlowAuditGuard, mocked mapper persistence. rejected before persistence write; rejection=500:仅状态为「已交付」的交付物可纳入成果转化包：QA delivered

### WF-029 · 成果包禁止绑定其他项目交付物 · FAIL

源码：`backend/src/main/java/com/comac/rpm/modules/transform/controller/TransformController.java`

Unmodified Java controllers + real FlowAuditGuard, mocked mapper persistence. Expected business rejection but call succeeded; writes=4;

### WF-030 · 成果不得由填报人直接改为完成绕过审核 · FAIL

源码：`backend/src/main/java/com/comac/rpm/modules/transform/controller/TransformController.java`

Unmodified Java controllers + real FlowAuditGuard, mocked mapper persistence. Expected business rejection but call succeeded; writes=1;

### WF-034 · 验收经费核销状态缺失不能视为完成 · FAIL

源码：`backend/src/main/java/com/comac/rpm/modules/acceptance/controller/AcceptanceController.java`

Unmodified Java controllers + real FlowAuditGuard, mocked mapper persistence. Expected business rejection but call succeeded; writes=1;

### WF-035 · 里程碑填报接口不能直接改为已完成 · FAIL

源码：`backend/src/main/java/com/comac/rpm/modules/milestone/controller/MilestoneController.java`

Unmodified Java controllers + real FlowAuditGuard, mocked mapper persistence. Expected business rejection but call succeeded; writes=1;

### WF-090 · 真实数据库事务、SQL数据隔离及并发重复提交 · BLOCKED

源码：`FlowAuditGuard + mapper infrastructure`

Mapper使用单项目内存夹具；selectList/selectOne直接返回夹具，不执行SQL过滤。身份测试仅验证控制器/guard对指定人员的判断，不能证明数据库行级范围、Spring鉴权、事务/并发语义。

### WF-091 · 核销后总部看板同步与真实附件持久化 · BLOCKED

源码：`FundController + dashboard/storage integration`

本套验证控制器核销状态和待办生成；总部聚合查询、真实对象存储与完整前后端E2E需要隔离部署环境。

## 测试脚本的移植调整

`qa/v19/workflow-checks.mjs` 原先依赖仓库外的 `../../../V7/project/runtime/backend/rpm-backend.jar`，当前仓库不包含该文件。本次改为默认使用本次 Maven 构建的 `backend/target/rpm-backend-1.0.0.jar`，并支持 `--runtime` 显式指定；断言未修改。输出保存在 deploy/logs，仓库原有已跟踪的历史结果保持原样。

## 验收边界

尚未执行全部真实业务状态的浏览器端到端流程、并发事务测试、备份恢复演练，以及 CMOS/邮箱/蓝信外部集成。完成本地部署不表示 V19 所有需求已验收通过。
